# Thông tin đăng nhập nginx basic-auth
AUTH_USER=admin
AUTH_PASS=changeme

# Token bí mật dùng cho cookie "nhớ đăng nhập" (đừng để lộ). Tạo mới: openssl rand -hex 32
AUTH_TOKEN=please-generate-with-openssl-rand-hex-32
